import React, {FC, ReactElement} from 'react'
import { View, Text} from 'react-native'

interface Props {
    title: string; 
}

const Test: FC<Props> = (props) => { 
    return(
        <View>
            <Text></Text>        </View>
    )
  };

